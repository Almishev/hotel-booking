# 🔧 Поправка на Next.js Routing в Render

## Проблем

Маршрутът `/register` (и други маршрути) не работят в облака, но работят на localhost.

## Причина

Next.js приложенията **НЕ МОГАТ** да се deploy-ват като **Static Site** в Render. Те трябва да се deploy-ват като **Web Service**, защото използват server-side rendering и нуждаят от Node.js сървър.

## Решение

### Стъпка 1: Проверете текущата конфигурация в Render

1. Отидете в [Render Dashboard](https://dashboard.render.com)
2. Намерете вашия frontend service
3. Проверете дали е конфигуриран като **Static Site** или **Web Service**

### Стъпка 2: Ако е Static Site - Премахнете и създайте отново като Web Service

**Важно:** Ако frontend-ът е конфигуриран като Static Site, трябва да го изтриете и да създадете нов Web Service.

1. **Изтрийте** текущия Static Site service
2. Създайте **нов Web Service**:
   - **New** → **Web Service**
   - Свържете GitHub repository
   - **Root Directory**: `frontend`
   - **Environment**: `Node`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`

### Стъпка 3: Environment Variables

Добавете следните environment variables в Render:

```
NEXT_PUBLIC_API_URL=https://your-backend-url.onrender.com
PORT=10000
```

**Важно:**
- Заменете `NEXT_PUBLIC_API_URL` с вашия backend URL
- `PORT` трябва да е `10000` за Render (или каквото Render назначи)

### Стъпка 4: Проверете конфигурацията

Уверете се, че `next.config.ts` съдържа:

```typescript
const nextConfig: NextConfig = {
  outputFileTracingRoot: path.join(__dirname, '../../'),
  output: 'standalone',
  trailingSlash: false,
  images: {
    unoptimized: false,
  },
};
```

Това вече е актуализирано в проекта.

### Стъпка 5: Deploy

1. Commit и push промените (ако има такива)
2. Render автоматично ще redeploy service-а
3. Изчакайте build процеса да завърши
4. Тествайте маршрутите (напр. `/register`, `/login`)

## Проверка

След deploy, проверете:

1. ✅ Главната страница работи: `https://your-frontend.onrender.com/`
2. ✅ `/register` маршрутът работи: `https://your-frontend.onrender.com/register`
3. ✅ `/login` маршрутът работи: `https://your-frontend.onrender.com/login`
4. ✅ Други маршрути работят правилно

## Допълнителни бележки

- **Web Service** в Render изисква Node.js сървър, който обработва заявките
- **Static Site** работи само за статични файлове и не поддържа server-side rendering
- Next.js App Router изисква server-side rendering за правилно routing
- `output: 'standalone'` оптимизира build-а за production deployment

## Troubleshooting

### Ако маршрутите все още не работят:

1. Проверете **Logs** в Render Dashboard за грешки
2. Проверете дали build процесът е успешен
3. Проверете дали `PORT` environment variable е правилно зададен
4. Проверете дали `NEXT_PUBLIC_API_URL` е правилно зададен

### Ако виждате "Not Found" за всички маршрути:

- Уверете се, че използвате **Web Service**, не Static Site
- Проверете дали `Start Command` е `npm start`
- Проверете дали build процесът е успешен
