# Phegon Hotel - Админски Flow и Описание на Приложението

## 📋 Какво е това приложение?

**Phegon Hotel** е **уеб-базирана система за управление на хотелски резервации (Hotel Management System - HMS)** или по-точно:

- **Уеб-приложение (Web Application)** - достъпно през браузър
- **Система за управление на хотелски бизнес (Hotel Management System)**
- **Платформа за онлайн резервации (Online Booking Platform)**
- **Full-stack уеб-приложение** - с frontend (Next.js) и backend (Spring Boot)

**Технически термин:** SaaS (Software as a Service) уеб-приложение за управление на хотелски операции

---

## 🔐 Админска автентикация и достъп

### Стъпка 1: Вход в системата
1. Админът отива на `/login`
2. Въвежда имейл и парола
3. Backend проверява credentials и генерира JWT токен
4. Системата проверява ролята на потребителя:
   - Ако ролята е `ADMIN` → достъп до админски панел
   - Ако ролята е `USER` → достъп само до потребителски функции
5. JWT токенът се запазва в `localStorage` заедно с ролята

### Стъпка 2: Защита на админските маршрути
- Всички админски страници са защитени с `AdminRoute` компонент
- Компонентът проверява:
  - Дали потребителят е автентикиран (`isAuthenticated()`)
  - Дали ролята е `ADMIN` (`isAdmin()`)
- Ако няма права → автоматично пренасочване към `/login`

### Стъпка 3: Backend защита
- Backend използва Spring Security с `@PreAuthorize("hasAuthority('ADMIN')")`
- Всички админски endpoints изискват валиден JWT токен с ADMIN роля

---

## 🎯 Пълен Flow на Админските Функционалности

### 🏠 **1. Админски Дашборд** (`/admin`)

**Какво вижда админът:**
- Персонализирано поздравление: "Добре дошъл, [Име на админ]"
- Два основни бутона:
  - **"Управлявай стаи"** → `/admin/manage-rooms`
  - **"Управлявай резервации"** → `/admin/manage-bookings`

**Функционалност:**
- Централна точка за достъп до всички админски функции
- Автоматично извличане на името на админа от профила

---

### 🏨 **2. Управление на стаи** (`/admin/manage-rooms`)

#### 2.1. Преглед на всички стаи
- **Списък с всички стаи** в системата
- **Пагинация** - по 5 стаи на страница
- **Филтриране по тип стая:**
  - Всички стаи
  - Delux
  - Standard
  - Suite
  - Или друг тип

#### 2.2. Добавяне на нова стая (`/admin/add-room`)
**Процес:**
1. Админът кликва на бутон "Добави нова стая"
2. Попълва формата:
   - **Тип стая:** Избор от съществуващи типове ИЛИ въвеждане на нов тип
   - **Цена на стая:** Числова стойност
   - **Описание:** Текстово описание на стаята
   - **Снимка:** Качване на изображение (до 2GB)
3. Preview на снимката преди запазване
4. При успешно добавяне → автоматично пренасочване към списъка със стаи

**Backend процес:**
- Снимката се качва в Cloudinary (облачно съхранение)
- URL на снимката се запазва в базата данни
- Стаята се добавя в PostgreSQL базата данни

#### 2.3. Редактиране на стая (`/admin/edit-room/[roomId]`)
**Процес:**
1. От списъка със стаи → кликване на "Редактирай стая"
2. Формата се попълва автоматично с текущите данни
3. Админът може да променя:
   - Тип стая
   - Цена
   - Описание
   - Снимка (опционално - ако не се качи нова, остава старата)
4. Бутони:
   - **"Обнови"** → запазва промените
   - **"Изтрий"** → изтрива стаята (с потвърждение)

#### 2.4. Изтриване на стая
- Потвърждение преди изтриване
- При изтриване се премахва от базата данни
- Автоматично пренасочване към списъка със стаи

---

### 📋 **3. Управление на резервации** (`/admin/manage-bookings`)

#### 3.1. Админски дашборд с статистика
**Реално време статистика:**
- **Общо резервации:** Брой на всички резервации
- **Общо гости:** Общ брой гости (възрастни + деца)
  - Детайли: X възрастни, Y деца
- **Общо приходи:** Изчислена обща сума от всички резервации
- **Активни резервации:** Брой на бъдещи резервации

#### 3.2. Аналитика по периоди
**Функционалност:**
1. Админът избира период:
   - Дата от
   - Дата до
2. Системата показва:
   - Резервации за периода
   - Възрастни за периода
   - Деца за периода
   - Общо гости за периода

#### 3.3. Разширени филтри
**Филтриране по:**
- **Търсене:** По код на резервация, име на гост или имейл
- **Дата от:** Начална дата за филтриране
- **Дата до:** Крайна дата за филтриране
- **Тип стая:** Delux, Standard, Suite, или Всички
- **Брой гости:** 1, 2, 3, 4, 5+ или Всички
- **Статус:**
  - **Активни** - резервации с check-in в бъдеще
  - **Текущи** - резервации, които са активни в момента
  - **Завършени** - резервации с check-out в миналото

#### 3.4. Сортиране
**Сортиране по колони:**
- Код на резервация (↑↓)
- Име на гост (↑↓)
- Дата на настаняване (↑↓)
- Дата на освобождаване (↑↓)
- Брой гости (↑↓)

#### 3.5. Таблица с резервации
**Показва:**
- Код на резервация
- Име на гост
- Дата на настаняване
- Дата на освобождаване
- Брой гости (възрастни + деца)
- Статус (с цветни badge-ове):
  - 🟢 Активни (зелен)
  - 🟡 Текущи (жълт)
  - 🔵 Завършени (син)
- Бутон "Управлявай" за всяка резервация

#### 3.6. Експорт на данни
- Бутон "Експорт на данни"
- Генерира CSV файл с всички филтрирани резервации
- Файлът включва:
  - Код на резервация
  - Име на гост
  - Имейл
  - Дати (check-in/check-out)
  - Брой гости
  - Тип стая
  - Статус

#### 3.7. Пагинация
- По 10 резервации на страница
- Навигация между страници

---

### ✏️ **4. Редактиране на резервация** (`/admin/edit-booking/[bookingCode]`)

**Процес:**
1. От таблицата с резервации → кликване на "Управлявай"
2. Страница с детайлна информация:

**Детайли за резервацията:**
- Код за потвърждение
- Дата на настаняване
- Дата на освобождаване
- Брой възрастни
- Брой деца

**Данни за клиента:**
- Име
- Имейл
- Телефонен номер

**Детайли за стаята:**
- Тип стая
- Цена
- Описание
- Снимка на стаята

**Действия:**
- **"Анулирай резервация"** (червен бутон):
  - Потвърждение преди анулиране
  - При успех → автоматично пренасочване към списъка с резервации

---

## 🔄 Пълен Workflow Пример

### Сценарий: Админ добавя нова стая и управлява резервации

1. **Вход в системата:**
   - Админът отива на `/login`
   - Въвежда credentials
   - Системата проверява ролята → ADMIN
   - Пренасочване към `/admin`

2. **Добавяне на нова стая:**
   - Кликва "Управлявай стаи"
   - Кликва "Добави нова стая"
   - Попълва формата (тип, цена, описание, снимка)
   - Кликва "Добави"
   - Стаята се добавя в базата данни
   - Автоматично пренасочване към списъка със стаи

3. **Преглед на резервации:**
   - Кликва "Управлявай резервации"
   - Вижда статистика в реално време
   - Прилага филтри (например: активни резервации)
   - Сортира по дата на настаняване

4. **Управление на конкретна резервация:**
   - Кликва "Управлявай" на конкретна резервация
   - Преглежда детайли за гост и стая
   - При нужда анулира резервация

5. **Експорт на данни:**
   - Прилага филтри за конкретен период
   - Кликва "Експорт на данни"
   - Сваля CSV файл с данните

---

## 🛡️ Сигурност и Защита

### Frontend защита:
- `AdminRoute` компонент проверява правата преди показване на страница
- Автоматично пренасочване при липса на права

### Backend защита:
- Spring Security с JWT токени
- `@PreAuthorize("hasAuthority('ADMIN')")` на всички админски endpoints
- Валидация на токени при всяка заявка

### База данни:
- PostgreSQL с JPA/Hibernate
- Автоматично хеширане на пароли
- Защита срещу SQL инжекции

---

## 📊 Технически детайли

### Frontend (Next.js):
- **Framework:** Next.js 16 с App Router
- **Език:** TypeScript
- **Стилизация:** Tailwind CSS + Custom CSS
- **Многоезичност:** i18next (български, английски, гръцки)
- **API комуникация:** Axios

### Backend (Spring Boot):
- **Framework:** Spring Boot 3.3.0
- **Език:** Java 21
- **База данни:** PostgreSQL (Supabase)
- **Автентикация:** JWT токени
- **Файлово съхранение:** Cloudinary
- **Email:** Spring Mail

### Интеграции:
- **Cloudinary** - за съхранение на снимки
- **Email Service** - за изпращане на потвърждения
- **PostgreSQL** - за съхранение на данни

---

## 🎯 Ключови предимства на админския панел

1. **Централизирано управление** - всички функции на едно място
2. **Реално време статистика** - виждане на данни веднага
3. **Разширени филтри** - бързо намиране на нужна информация
4. **Експорт на данни** - за външна обработка и отчети
5. **Интуитивен интерфейс** - лесно за използване
6. **Многоезична поддръжка** - достъпно на 3 езика
7. **Responsive дизайн** - работи на всички устройства
8. **Сигурност** - защитено с JWT и роли

---

## 📝 Терминология за представяне

### Официално описание:
**"Phegon Hotel е уеб-базирана система за управление на хотелски резервации (Hotel Management System), която предоставя пълнофункционален админски панел за управление на стаи, резервации и аналитика на бизнеса."**

### Кратко описание:
**"Уеб-приложение за управление на хотелски бизнес с админски панел за управление на стаи и резервации."**

### Техническо описание:
**"Full-stack уеб-приложение (Next.js + Spring Boot) за управление на хотелски операции с RESTful API, JWT автентикация и PostgreSQL база данни."**

---

## 🚀 Бъдещи подобрения (опционално)

- Интеграция с платежни системи
- Система за отзиви и рейтинги
- Интеграция с channel managers
- Push уведомления
- Mobile приложение
- Интеграция с POS системи
- Автоматични отчети по email
- Календарен изглед на резервациите

---

**Документът е създаден на:** $(date)
**Версия:** 1.0
