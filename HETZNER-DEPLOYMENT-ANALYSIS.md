# 📚 Обучителен анализ: Деплой на проект в Hetzner

Този документ описва пълния процес на деплой на Phegon Hotel приложение в Hetzner Cloud, включително всички проблеми, които срещнахме и как ги решихме.

## 🎯 Цел на проекта

Деплой на full-stack приложение (Spring Boot backend + Next.js frontend + Nginx reverse proxy) в Hetzner Cloud сървър, използвайки Docker и Docker Compose.

---

## 📋 Стъпка 1: Избор на сървър в Hetzner

### Какво направихме:
1. Създадохме Cloud Server в Hetzner Console
2. Избрахме **CX23** (Cost-Optimized) за €3.59/месец
3. Избрахме Ubuntu 24.04 LTS
4. Локация: Helsinki (eu-central)

### Важни уроци:

#### ✅ Правилният избор на сървър тип:
- **Cost-Optimized (CX23)**: Подходящ за тестване и образователни цели
  - Плюсове: Много евтин (€3.59/месец)
  - Минуси: Променлива CPU производителност, ограничена наличност
  - Кога да използваш: За тестване, development, малък трафик

- **Regular Performance (CPX)**: Подходящ за production
  - Плюсове: Стабилна производителност, гарантирани ресурси
  - Минуси: По-скъпо (€7-8/месец)
  - Кога да използваш: За production приложения

#### 💡 Урок:
За образователна цел и тестване, Cost-Optimized е достатъчен. За production, използвай Regular Performance или General Purpose.

---

## 📋 Стъпка 2: Свързване към сървъра

### Какво направихме:
```bash
ssh root@46.224.183.244
```

### Важни уроци:

#### ✅ Безопасност:
- Винаги използвай SSH ключове вместо пароли (по-сигурно)
- Промени root паролата при първо влизане
- Настрой firewall преди да стартираш приложението

#### 💡 Урок:
SSH ключовете са по-сигурни от пароли. За production, винаги използвай SSH ключове.

---

## 📋 Стъпка 3: Инсталиране на необходимите инструменти

### Какво направихме:
```bash
# Обновяване на системата
apt update && apt upgrade -y

# Инсталиране на Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Инсталиране на Docker Compose
apt-get install docker-compose-plugin -y

# Инсталиране на Git
apt-get install git -y
```

### Важни уроци:

#### ✅ Проверка след инсталация:
Винаги провери дали инструментите са инсталирани правилно:
```bash
docker --version
docker compose version
git --version
```

#### 💡 Урок:
Винаги проверявай версиите след инсталация, за да се увериш, че всичко е наред.

---

## 📋 Стъпка 4: Клониране на проекта

### Какво направихме:
```bash
git clone https://github.com/Almishev/hotel-booking.git
cd hotel-booking
```

### Проблем, който срещнахме:
- Очаквахме директорията да се казва `hms`, но се казваше `hotel-booking`

### Решение:
```bash
cd hotel-booking  # вместо cd hms
```

### Важни уроци:

#### ✅ Проверка на структурата:
Винаги провери структурата на проекта след клониране:
```bash
ls -la
cat docker-compose.yml  # провери дали съществува
```

#### 💡 Урок:
Винаги проверявай името на директорията след `git clone`. Може да е различно от очакваното.

---

## 📋 Стъпка 5: Конфигуриране на environment variables

### Какво направихме:
Създадохме `.env` файл с всички необходими променливи:

```env
# Backend - Database (Supabase)
SPRING_DATASOURCE_URL=jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:5432/postgres?user=postgres.plbbjyxhlcruxtmyxcjy&password=Asroma%40123&sslmode=require

# Backend - CORS
CORS_ALLOWED_ORIGINS=http://46.224.183.244,https://46.224.183.244

# Frontend - API URL
NEXT_PUBLIC_API_URL=http://46.224.183.244/api
NEXT_PUBLIC_SITE_URL=http://46.224.183.244
```

### Важни уроци:

#### ✅ Ключови точки:
1. **CORS_ALLOWED_ORIGINS**: Трябва да включва IP адреса на сървъра
2. **NEXT_PUBLIC_API_URL**: Трябва да сочи към правилния API endpoint (през Nginx)
3. **NEXT_PUBLIC_SITE_URL**: Трябва да сочи към правилния домейн/IP

#### 💡 Урок:
Environment variables са критични за правилната работа на приложението. Винаги проверявай дали са правилно конфигурирани.

---

## 📋 Стъпка 6: Настройка на Firewall

### Какво направихме:
```bash
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw enable
```

### Важни уроци:

#### ✅ Ред на действия:
1. Винаги отвори SSH порт (22) ПРЕДИ да активираш firewall
2. След това отвори портовете за приложението (80, 443)
3. Накрая активирай firewall

#### 💡 Урок:
Ако активираш firewall преди да отвориш SSH порт, може да се заключиш извън сървъра!

---

## 📋 Стъпка 7: Първи build и стартиране

### Какво направихме:
```bash
docker compose up -d --build
```

### Резултат:
- Build-ът приключи успешно
- Всички контейнери стартираха
- Frontend се зареди в браузъра

### Проблем, който срещнахме:
Frontend-ът използваше `http://localhost/api` вместо `http://46.224.183.244/api`

---

## 🐛 Проблем 1: Frontend използва localhost вместо IP адрес

### Симптоми:
- В браузъра: `Access to XMLHttpRequest at 'http://localhost/api/...' has been blocked by CORS policy`
- Грешка: `ERR_CONNECTION_REFUSED`

### Причина:
Next.js вгражда `NEXT_PUBLIC_*` environment variables **по време на build**, не по време на runtime. Тъй като build-ът беше направен преди да се създаде `.env` файлът, или environment variables не бяха предадени правилно по време на build, Next.js използваше fallback стойността `http://localhost/api`.

### Решение:

#### Стъпка 1: Поправка на `frontend/Dockerfile`
Добавихме `ARG` и `ENV` в build stage-а:

```dockerfile
# Accept build arguments for Next.js public environment variables
ARG NEXT_PUBLIC_API_URL
ARG NEXT_PUBLIC_SITE_URL

# Set environment variables for build
ENV NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
ENV NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL}

# Build the Next.js application
RUN npm run build
```

#### Стъпка 2: Поправка на `docker-compose.yml`
Добавихме `args:` секция в build конфигурацията:

```yaml
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost/api}
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-http://localhost}
```

#### Стъпка 3: Rebuild без кеш
```bash
docker compose down
docker compose build --no-cache frontend
docker compose up -d
```

### Важни уроци:

#### ✅ Next.js Environment Variables:
- `NEXT_PUBLIC_*` променливи се вграждат **по време на build**
- Трябва да се предават като `ARG` в Dockerfile
- Трябва да се предават като `build args` в docker-compose.yml
- Rebuild без кеш е необходим, за да се използват новите стойности

#### 💡 Урок:
Когато работиш с Next.js в Docker, винаги предавай `NEXT_PUBLIC_*` променливи като build arguments, не само като runtime environment variables.

---

## 🐛 Проблем 2: YAML синтактична грешка в docker-compose.yml

### Симптоми:
```
failed to parse /root/hotel-booking/docker-compose.yml: yaml: unmarshal errors:
  line 1: line 42: mapping key "build" already defined at line 5
```

### Причина:
При ръчно редактиране на файла, вероятно е допусната грешка и е създаден дублиран ключ.

### Решение:
```bash
git checkout docker-compose.yml
git pull
```

### Важни уроци:

#### ✅ Git като backup:
- Винаги commit-вай промените в Git преди да ги прилагаш на сървъра
- Използвай `git pull` за да синхронизираш промените
- Ако направиш грешка, можеш да върнеш файла с `git checkout`

#### 💡 Урок:
Git не е само за версиониране - е и отличен начин за backup и възстановяване на файлове.

---

## 📋 Стъпка 8: Финален rebuild и тестване

### Какво направихме:
```bash
# Презаписване на файловете от GitHub
git reset --hard HEAD
git pull

# Проверка на синтаксиса
docker compose config --quiet

# Rebuild без кеш
docker compose down
docker compose build --no-cache frontend
docker compose up -d
```

### Резултат:
✅ Frontend-ът използва правилния API URL
✅ Няма CORS грешки
✅ Приложението работи правилно

---

## 🎓 Ключови уроци и Best Practices

### 1. Environment Variables в Next.js

#### ❌ Грешка:
```yaml
# docker-compose.yml
frontend:
  environment:
    NEXT_PUBLIC_API_URL: http://46.224.183.244/api
```
Това НЕ работи, защото Next.js вгражда променливите по време на build!

#### ✅ Правилно:
```yaml
# docker-compose.yml
frontend:
  build:
    args:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
  environment:
    NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}  # все пак е добре да го има и тук
```

```dockerfile
# Dockerfile
ARG NEXT_PUBLIC_API_URL
ENV NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
RUN npm run build
```

### 2. Docker Build Cache

#### ❌ Грешка:
```bash
docker compose build frontend
```
Ако имаш кеширани слоеве, новите environment variables няма да се използват!

#### ✅ Правилно:
```bash
docker compose build --no-cache frontend
```
Използвай `--no-cache` когато променяш build-time environment variables.

### 3. CORS Configuration

#### ❌ Грешка:
```env
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:3000
```
Това работи само локално!

#### ✅ Правилно:
```env
CORS_ALLOWED_ORIGINS=http://46.224.183.244,https://46.224.183.244
```
Винаги включи IP адреса или домейна на сървъра в CORS.

### 4. Git Workflow

#### ✅ Best Practice:
1. Направи промените локално
2. Тествай локално
3. Commit и push в GitHub
4. На сървъра: `git pull`
5. Rebuild контейнерите

#### 💡 Урок:
Не редактирай файлове директно на сървъра. Винаги прави промените локално, commit-вай ги, и след това pull-ни на сървъра.

### 5. Firewall Setup

#### ✅ Правилен ред:
1. Отвори SSH порт (22) ПРЕДИ да активираш firewall
2. Отвори портовете за приложението (80, 443)
3. Активирай firewall

#### ❌ Грешка:
Активиране на firewall преди да отвориш SSH порт = заключване извън сървъра!

---

## 🔍 Често срещани проблеми и решения

### Проблем 1: "Cannot find module '/app/server.js'"

**Причина:** Next.js standalone output не е правилно конфигуриран.

**Решение:** Провери `next.config.ts`:
```typescript
const nextConfig = {
  output: 'standalone',
};
```

### Проблем 2: "Network Error" или "ERR_CONNECTION_REFUSED"

**Причина:** Frontend използва `localhost` вместо правилния URL.

**Решение:** 
1. Провери `NEXT_PUBLIC_API_URL` в `.env`
2. Увери се, че се предава като build arg
3. Rebuild без кеш

### Проблем 3: CORS грешки

**Причина:** Backend не позволява заявки от frontend origin.

**Решение:**
1. Провери `CORS_ALLOWED_ORIGINS` в `.env`
2. Увери се, че включва IP адреса или домейна на сървъра
3. Рестартирай backend контейнера

### Проблем 4: "502 Bad Gateway" от Nginx

**Причина:** Backend или frontend контейнерите не са стартирали.

**Решение:**
```bash
docker compose ps  # провери статуса
docker compose logs backend  # провери логовете
docker compose logs frontend
```

### Проблем 5: YAML синтактична грешка

**Причина:** Дублиран ключ или неправилна индентация.

**Решение:**
```bash
docker compose config --quiet  # провери синтаксиса
git checkout <file>  # възстанови от Git
```

---

## 📊 Времева линия на деплоя

1. **Създаване на сървър**: ~2 минути
2. **Инсталиране на инструменти**: ~5 минути
3. **Клониране на проект**: ~1 минута
4. **Конфигуриране на .env**: ~2 минути
5. **Първи build**: ~10 минути
6. **Откриване на проблем с environment variables**: ~30 минути
7. **Поправка на Dockerfile и docker-compose.yml**: ~15 минути
8. **Rebuild и тестване**: ~5 минути

**Общо време**: ~1 час (включително troubleshooting)

---

## 🎯 Ключови изводи

### 1. Next.js и Docker
- `NEXT_PUBLIC_*` променливи трябва да се предават като build arguments
- Винаги използвай `--no-cache` при промяна на build-time variables
- Проверявай environment variables в build-ания код

### 2. Git Workflow
- Винаги commit-вай промените преди да ги прилагаш на сървъра
- Използвай `git pull` за синхронизация
- Git е отличен backup механизъм

### 3. Environment Variables
- Разлика между build-time и runtime variables
- Next.js вгражда `NEXT_PUBLIC_*` по време на build
- Винаги проверявай `.env` файла

### 4. Troubleshooting
- Винаги проверявай логовете: `docker compose logs`
- Проверявай статуса: `docker compose ps`
- Тествай синтаксиса: `docker compose config --quiet`

### 5. Security
- Винаги настрой firewall правилно
- Използвай SSH ключове вместо пароли
- Ограничи CORS origins само до необходимите

---

## 📚 Полезни команди

### Docker Compose
```bash
# Стартиране
docker compose up -d --build

# Спиране
docker compose down

# Rebuild без кеш
docker compose build --no-cache <service>

# Проверка на статуса
docker compose ps

# Преглед на логове
docker compose logs -f <service>

# Проверка на синтаксиса
docker compose config --quiet
```

### Git
```bash
# Изтегляне на промени
git pull

# Възстановяване на файл
git checkout <file>

# Презаписване на всички промени
git reset --hard HEAD
```

### System
```bash
# Проверка на firewall
ufw status

# Проверка на отворените портове
netstat -tulpn | grep -E '80|443'

# Проверка на дисковото пространство
df -h
```

---

## 🎓 Заключение

Деплойът на проект в Hetzner изисква внимателно планиране и разбиране на:
- Как Docker работи с build-time и runtime environment variables
- Как Next.js вгражда environment variables
- Как да конфигурираш CORS правилно
- Как да използваш Git за синхронизация и backup

Следвайки тези уроци и best practices, можеш да избегнеш често срещаните проблеми и да направиш успешен деплой.

---

## 📝 Checklist за следващ деплой

- [ ] Избрал правилния тип сървър за нуждите ти
- [ ] Инсталирал Docker и Docker Compose
- [ ] Клонирал проекта правилно
- [ ] Създал `.env` файл с правилните стойности
- [ ] Настроил firewall правилно (SSH първо!)
- [ ] Конфигурирал `Dockerfile` да предава build args
- [ ] Конфигурирал `docker-compose.yml` с build args
- [ ] Rebuild-нал без кеш след промени в environment variables
- [ ] Проверил CORS настройките
- [ ] Тествал приложението в браузъра
- [ ] Commit-нал промените в Git

---

**Успешен деплой!** 🎉

